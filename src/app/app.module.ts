/*
import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { HttpClientModule } from '@angular/common/http';
import { ReactiveFormsModule } from '@angular/forms';
import { AppComponent } from './app.component';
import { AppRoutingModule } from './app.routes'; // Assuming app.routes.ts contains the routing module
// Importing the AuthentificationControllerService from the correct path
import { AuthentificationControllerService } from './swaggerFiles';

@NgModule({
  declarations: [
    AppComponent,

  ],
  imports: [
    BrowserModule,
    HttpClientModule,
    ReactiveFormsModule,
    AppRoutingModule

  ],
  providers: [AuthentificationControllerService],
  bootstrap: [AppComponent]
})
export class AppModule { }*/
